<?xml version="1.0" encoding="UTF-8"?>
<Secret>
    <hash>{{ $secret->hash }}</hash>
    <secretText>{{ $secret->secretText }}</secretText>
    <createdAt>{{ $secret->createdAt }}</createdAt>
    <expiresAt>{{ $secret->expiresAt }}</expiresAt>
    <remainingViews>{{ $secret->remainingViews }}</remainingViews>
</Secret>
